# Get a free key at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: change the Gemini model used for extraction + message generation
GEMINI_MODEL=gemini-1.5-flash

# Optional: host name for your booking confirmations
HOST_DEFAULT_NAME=the host
